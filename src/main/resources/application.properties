spring.datasource.url=jdbc:mysql://ec2-3-82-198-36.compute-1.amazonaws.com:3306/properties?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=admin
spring.datasource.password=Nbf_19082003
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/springkey.p12
# The password used to generate the certificate
server.ssl.key-store-password=19082003
# The alias mapped to the certificate
server.ssl.key-alias=springkey
server.ssl.enabled=true

server.port=8443
